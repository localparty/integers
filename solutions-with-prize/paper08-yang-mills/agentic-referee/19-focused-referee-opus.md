# Focused Referee: Hot Zone Audit (Opus Max Effort)

You are a hostile mathematical referee. You assume the proof is wrong
until forced to concede otherwise. You do not give partial credit.

## Context

This proof has survived 15 referee rounds. The git history reveals
8 "hot zones" where errors cluster and fixes introduce new errors.
Your job is to audit ONLY these zones. Do not review the rest.

**The recurring failure mode:** Fixes to flagged issues introduce
new algebraic or notational errors, especially in:
- Exponent conversions (eigenvalue vs. mass vs. coupling powers)
- Inequality directions in Cauchy/perturbation estimates
- Volume factors that cancel in physics but not in formal bounds

Read ALL files listed below before writing anything.

---

## Files to Read

1. `/Users/gsix/yang-mills/preprint/sections/05-continuum-limit.md`
   (the entire file -- this is ground zero)
2. `/Users/gsix/yang-mills/preprint/sections/04-lattice-proof-part1.md`
   (focus: Theorem 2 Step 4, Theorem 5 Assembly)
3. `/Users/gsix/yang-mills/preprint/sections/I4-other-gauge-groups.md`
4. `/Users/gsix/yang-mills/preprint/sections/I3-N-dependence-tracking.md`
5. `/Users/gsix/yang-mills/preprint/sections/C-transfer-matrix.md`
   (focus: C.4.1 Feshbach factorization)

---

## The 8 Hot Zones

### HZ-1: Section 5.7(f) — OS Reconstruction & Non-Triviality
**Lines:** ~1970–2750 in `05-continuum-limit.md`
**History:** 4 rewrites of non-triviality argument, 25 diff hunks total.

Check these specific claims:
(a) The Haar variance computation: $\int (\mathrm{Re}\,\mathrm{Tr}(U))^2\,dU = 1/2$
    for $N \geq 3$. Verify by explicit character orthogonality.
(b) The spectral contradiction: if $n_* \geq 2$ then the temporal tube
    lower bound $C_1 e^{-\Delta t}$ contradicts the upper bound
    $\mathrm{Var}(s_P) e^{-2\Delta t}$. Is the division by
    $e^{-\Delta t}$ legitimate (i.e., does this step require $t \to \infty$
    or does it hold for finite $t$)?
(c) The Kato analyticity extension: $T(\beta)$ analytic for all
    $\beta \in (0, \beta_0)$ (Balaban CMP 95, Theorem 4.1). Does this
    reference actually exist and say what is claimed?
(d) The SD equation convergence: the lattice derivative
    $-\partial_{U_\ell}^a S_W = a^2 (D_\nu F^{a,\nu\mu})(x) + O(a^4)$.
    Verify the $a^2$ prefactor and the claim that the $O(a^4)$ correction
    is bounded by $Cg_k^4$.
(e) The "scope" paragraph: "In a confining theory with mass gap, the
    physical sector exhausts the full Hilbert space." Is this a theorem
    or a physical assertion?

### HZ-2: Section 5.5.3 — The Spectral Lemma
**Lines:** ~1056–1391 in `05-continuum-limit.md`
**History:** 3 rewrites. Algebraic bugs in coupling condition, constant definition.

Check:
(a) The coupling condition for $E_2 - E_1 \geq \hat{\Delta}_k/2$.
    The text should say $g_k^4 \leq 1/(4C_B)$, equivalently
    $g_k^2 \leq 1/(2\sqrt{C_B})$. Verify this algebra explicitly.
(b) The constant $C_* = e^{\hat{\Delta}_{\max}}$ in the near-diagonal
    bound. What is $\hat{\Delta}_{\max}$? Is it the supremum over all
    $k$? If $\hat{\Delta}_k = O(1)$ at $k = 0$, does $C_*$ depend on
    the initial lattice spacing $a$?
(c) The Combes-Thomas locality: $R_0$ is claimed $k$-independent.
    Trace this through Balaban's polymer expansion. Does the support
    radius of operators generated by block-spin grow with $k$ in
    physical units, even if it is bounded in block lattice units?
(d) The linear combination lemma (lines ~1310–1360): Does the
    "Remark (Uniform temporal extent)" added after the lemma correctly
    state that $R_i \leq R_0$ follows from Balaban CMP 109, Theorem 1?
    Is CMP 109 the right paper? Theorem 1 or Theorem 3?

### HZ-3: Section 5.1.3 — Effective Action Decomposition
**Lines:** ~63–108 in `05-continuum-limit.md`
**History:** 5 rounds flagged. Cauchy estimate derivation was wrong.

Check:
(a) The "Origin" paragraph for $|c_n^{(k)}| \leq C_n g_k^{d_n-4}$.
    It says "each field beyond those comprising $S_{\mathrm{YM}}$
    carries one power of $g_k$." Is this dimensionally correct?
    $S_{\mathrm{YM}}$ has dimension 0 (it's dimensionless). Each
    $F_{\mu\nu}$ has dimension 2. An operator of dimension $d_n$ has
    $d_n/2$ factors of $F$... The power counting argument needs to
    be more precise. Is it correct?
(b) The total remainder $\|E_k\| \leq C g_k^4$: is this per unit
    volume in block lattice units or physical units? If block lattice
    units, the block size grows as $2^k a$, so "per unit volume"
    means different things at different scales.

### HZ-4: Theorem 2 Step 4 — KK Mode Sum
**Lines:** ~510–540 in `04-lattice-proof-part1.md`
**History:** 3 versions of the Weyl exponent. Previous fixes were wrong.

Check:
(a) The current claim: $m_n \sim n^{1/(2(N-1))}/r_3$. Verify from
    first principles: Weyl's law on $\mathbb{CP}^{N-1}$ ($d = 2(N-1)$)
    gives $\lambda_n \sim n^{2/d} = n^{1/(N-1)}$. Then
    $m_n = \sqrt{\lambda_n}/r_3 \sim n^{1/(2(N-1))}/r_3$. But wait:
    line 518 says "$m_n = \sqrt{\lambda_n}$" without the $r_3$. Is the
    $r_3$ in $\lambda_n$ or in $m_n$? Check against line 418 which
    defines $m_n = \sqrt{\lambda_n^{(1)}}/r_3$.
(b) The degeneracy $d_n \sim n^{N-2}$: Weyl's law gives the counting
    function $N(\lambda) \sim \lambda^{d/2}$. The degeneracy of the
    $n$-th eigenvalue is $d_n = N(\lambda_n) - N(\lambda_{n-1}) \sim
    \lambda_n^{d/2-1} \cdot (\lambda_n - \lambda_{n-1})$. Is $n^{N-2}$
    the correct growth rate?
(c) The convergence claim: does $\sum n^{N-2} e^{-c n^{1/(2(N-1))} a/r_3}$
    converge for all $N \geq 2$? For $N = 2$: $\sum e^{-c n^{1/2} a/r_3}$.
    For $N = 3$: $\sum n \cdot e^{-c n^{1/4} a/r_3}$. Yes, these converge
    for $a/r_3 > 0$, but verify the rate.

### HZ-5: Theorem 5 Assembly — Min-Max Argument
**Lines:** ~1220–1290 in `04-lattice-proof-part1.md`
**History:** 2 rewrites. Switched from trace-norm to min-max.

Check:
(a) The operator inequality $(1-\bar{\epsilon})T_{\mathrm{std}} \leq
    T_{\mathrm{red}} \leq (1+\bar{\epsilon})T_{\mathrm{std}}$: this
    follows from the pointwise kernel bound, but pointwise kernel
    inequalities imply operator inequalities ONLY for integral operators
    with non-negative kernels. Are both $T_{\mathrm{red}}$ and
    $T_{\mathrm{std}}$ non-negative kernels? (They should be, by
    reflection positivity.)
(b) The min-max application: Reed-Simon Vol. IV, Theorem XIII.1. Does
    this theorem apply to trace-class operators, or only to bounded
    operators? The transfer matrices are trace-class. Is min-max valid?
(c) The eigenvalue ratio bound: the proof writes
    $\lambda_1(T_{\mathrm{red}})/\lambda_0(T_{\mathrm{red}}) \in
    [(1-\bar\epsilon)/(1+\bar\epsilon), (1+\bar\epsilon)/(1-\bar\epsilon)]
    \cdot \lambda_1(T_{\mathrm{std}})/\lambda_0(T_{\mathrm{std}})$.
    But min-max gives bounds on INDIVIDUAL eigenvalues, not ratios.
    The ratio bound requires BOTH eigenvalues to be perturbed in the
    same direction. Is this justified? (It should be — both are
    squeezed by the same factor — but state it.)

### HZ-6: Appendix C.4.1 — Feshbach Eigenstate Factorization
**Lines:** the new section in `C-transfer-matrix.md`
**History:** Added in r15 to close B3. Never verified.

Check:
(a) The bound $\|Q_0|n\rangle\| \leq 2C_W e^{-m_1 a}/m_1 \cdot
    \|P_0|n\rangle\|$: verify the resolvent bound
    $\|(E_n - H_{QQ})^{-1}\| \leq 2/m_1$. This uses
    $E_n < \Delta_0^{\mathrm{KK}} \ll m_1/2$. Is $m_1/2$ the correct
    threshold (not $m_1$)?
(b) The Bach-Froehlich-Sigal reference (1998, Theorem 2.1): does it
    actually state isospectrality of the Feshbach map below the
    essential spectrum threshold?
(c) The factorization applies to $n = 0$ (vacuum) and $n = 1$
    (glueball). For $n = 1$: the glueball is the FIRST EXCITED STATE
    of $\hat{H}$, not the ground state. The Feshbach map gives a
    bijection for eigenvalues below $\inf\sigma(H_{QQ})$. Is $E_1$
    guaranteed to be below this threshold? (It should be, since
    $E_1 = \Delta_0^{\mathrm{KK}} \ll m_1$.)

### HZ-7: I4 — Einstein Constants for Exceptional Groups
**Lines:** ~206–310 in `I4-other-gauge-groups.md`
**History:** 3 edits to G_2 derivation; 7 rounds flagged.

Check:
(a) $G_2$: $\lambda_{G_2} = 4/r_3^2$. The stated Casimir eigenvalue
    $c_2(\mathfrak{m}) = 4$ for the 8-dim isotropy representation.
    Is the normalization correct? Helgason Table VI uses the Killing
    form normalization. The paper uses Fubini-Study-type normalization.
    Are these the same for $G_2/\mathrm{SO}(4)$?
(b) $E_8$: $\lambda_{E_8} = 30/r_3^2$. The independent verification
    via restricted roots claims $(m_\alpha, m_{2\alpha}) = (112, 15)$.
    Verify: $\dim(E_8/\mathrm{SO}(16)) = 128$. The restricted root
    system of type $BC_1$ has multiplicities summing to
    $m_\alpha + m_{2\alpha} = 127$ (= dim - 1 for rank 1). Is
    $112 + 15 = 127$? Yes. Good. But is the $BC_1$ restricted root
    system correct for this pair? (It should be — EVIII is a rank-1
    symmetric space... wait, is it? $E_8/\mathrm{SO}(16)$ has rank 1?)
(c) $E_6$, $E_7$: the derivation uses $\lambda = 2h^\vee/\dim_\mathbb{C}$
    (Besse 1987, Theorem 8.96). Is this theorem actually about
    Hermitian symmetric spaces? Does it apply with this exact formula?

### HZ-8: I3 — N-Dependence Tracking
**File:** `I3-N-dependence-tracking.md`
**History:** 4 rounds flagged. Contradictory $\gamma$ values.

Check:
(a) The Gronwall exponent $\gamma$: is it stated consistently throughout
    the appendix and in Section 5.4.6? The history shows a contradiction
    ($\gamma \sim N$ in one place, $\gamma \sim 1/N$ in another).
(b) The Weitzenboeck eigenvalue: the appendix should use
    $\mu_1 \geq 2N/r_3^2$ (Weitzenboeck bound) or $4N/r_3^2$ (exact
    Ikeda-Taniguchi value). Which one? Is it consistent with Appendix A
    and Theorem 1?
(c) The accumulated error bound: with ~14 steps each carrying $O(N^2)$
    constants, does the product stay finite for each fixed $N$? The
    claim is that the geometric contraction $1/4$ beats any polynomial.
    Verify this for the worst case ($N = 2$, or large $N$).

---

## Output

Write one `.md` file per hot zone to `/Users/gsix/yang-mills/math-referee/`:
- `hz-1-os-reconstruction.md`
- `hz-2-spectral-lemma.md`
- `hz-3-effective-action.md`
- `hz-4-kk-mode-sum.md`
- `hz-5-theorem5-assembly.md`
- `hz-6-feshbach-factorization.md`
- `hz-7-einstein-constants.md`
- `hz-8-n-dependence.md`

Plus `hz-summary.md` with the overall tally.

For each hot zone, verdict is one of:
- **CLEAN** — no errors found
- **NOTATION ERROR** — wrong symbol/exponent but argument survives
- **ALGEBRAIC ERROR** — incorrect inequality/bound
- **LOGICAL GAP** — missing step or circular reasoning
- **LOAD-BEARING ERROR** — breaks the proof chain

Focus on ALGEBRA. The conceptual structure has survived 15 rounds.
What keeps failing is the arithmetic.

## Pipeline integration

This is Phase 1 of a 3-phase pipeline (see
`/Users/gsix/yang-mills/etc/21-agentic-pipeline.md`):
- Phase 1a (this prompt): you find issues
- Phase 1b: when the user says "address each gap", you apply
  surgical fixes
- Phase 2 (Sonnet, separate session): verifies your fixes for
  algebraic correctness, stale references, and regressions
- Phase 3 (you again): meta-verify Sonnet's report and either
  auto-commit OR halt

**DO NOT COMMIT in Phase 1a or 1b.** Commits happen only after
Phase 2 returns clean. If your fix touches a load-bearing claim
(the proof chain $\Delta_0^{\mathrm{KK}} > 0 \to \Delta_0^{\mathrm{std}}
> 0 \to \Delta_\infty > 0$), say so explicitly so the user can
review before any commit.

**Stale-reference rule:** When you fix a formula, immediately grep
the entire `preprint/sections/` directory for the OLD form and
update every occurrence. The most common fix-introduces-error pattern
is leaving stale copies in summary tables, numerical estimates, and
proof-chain ledgers.
