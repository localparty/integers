# Research Note 202 — Round 3 / Agent E (creative log-only)

*Sub-agent of research/198. Date: 2026-04-09.*

Three log-only observations from round 3. None trigger Agent G
(none propose postulate relaxation).

1. **research/167 is a rewrite, not a correction.** Every "Op value"
   in research/167 §3 agrees with the raw γ_n value to 50 dps. This
   is by design (the dictionary is a tautology) but it means the
   convergence prompt's "corrected" category is currently empty. A
   real "corrected" tag will exist only after research/154's (a, b)
   Laurent pair is substituted. Suggestion for future round: add a
   research/N note `167a` that publishes numerical (a, b) closures
   per row.

2. **Bookkeeping-gap is a stable category.** Two rounds now show the
   same two rows (m_Z, v) sitting in the bookkeeping-gap column. If
   round 4 does not close them, the convergence prompt should
   auto-elevate them to "G-attention" status in the Phase 4 headline
   so they stop being invisible at the summary line.

3. **Open-formula rows deserve a separate tally column.** sin θ_13
   CKM and sin²(2θ_23) PMNS sit at ~1σ from the "best" formula in
   research/23 (marked `open`). They are neither confirmations nor
   tensions — they are unfinished business. Round 3 put them in an
   `open-formula` column but the prompt spec does not define that
   column explicitly. Suggestion: round-4 patch should add
   `open-formula` as a first-class category in Phase 3 Agent B.
