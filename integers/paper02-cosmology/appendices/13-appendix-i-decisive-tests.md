# Appendix I — Decisive Tests: The Framework's Experimental Roadmap

> Eight experiments over the next 5–10 years will either confirm
> or falsify every major prediction of the 5D framework's
> cosmological sector. The most decisive is CMB-S4: a measurement
> of `N_eff = 3.39 ± 0.03` would be a `10σ` detection of the mirror
> sector. This appendix provides the full experimental roadmap.

---

## I.1 The Complete Prediction Set

From the CAMB computation (Appendix A). Three scenarios bracket the
framework's prediction; the table shows both Scenario A (`θ*`-matched,
`ξ = 0.47`) and Scenario B (`1/ξ²` law, `ξ = 0.432`). Values shared by
both scenarios are listed once.

| Observable | Scenario A (`ξ=0.47`) | Scenario B (`ξ=0.432`) | Planck `ΛCDM` | Experiment |
|-----------|--------------------|--------------------|-------------|------------|
| `t₀` (Gyr) | **13.47** | **13.47** | 13.797 | Stellar ages (JWST) |
| `H₀` (km/s/Mpc) | **69.5** | **68.7** | 67.4 | TRGB, GW sirens |
| `N_eff` | **3.39** | **3.31** | 3.044 | CMB-S4 |
| `w₀` | **−1** | **−1** | −1.0 | DESI DR3, Euclid |
| `w_a` | **0** | **0** | 0.0 | DESI DR3, Euclid |
| `S8` | **0.753** | **0.785** | 0.832 | Euclid, Rubin |
| `σ₈` | **0.766** | **0.782** | 0.811 | CMB lensing |
| `Ω_m` | **0.290** | **0.302** | 0.315 | BAO + CMB |
| `r_d` (Mpc) | **146.2** | **145.8** | 147.1 | DESI DR3 |
| `θ*` offset | **−0.5"** | **+6.6"** | 0 | CMB-S4 |
| `Ω_DM/Ω_b` | 5.22 (input) | **5.36 (derived)** | — | — |
| `Σm_ν` (eV) | **0.060** | **0.060** | < 0.12 | CMB-S4, KATRIN |
| Mass ordering | **Normal** | **Normal** | unknown | JUNO |

---

## I.2 CMB-S4: The Most Decisive Test

**Target:** `σ(N_eff) ≈ 0.03`, `σ(θ*) ≈ 0.03 arcsec`
**Expected:** 2030
**Framework prediction:** `N_eff = 3.31–3.39` (Scenario B–A)

At CMB-S4 precision:
- `N_eff = 3.31–3.39` is a **`9–11.5σ` detection** above the SM value (3.046)
- `N_eff = 3.31–3.39` vs `N_eff = 3.09` (tower-only) is **`7–10σ`** — distinguishes
  whether the mirror sector exists
- `θ*` offset of −0.5" (Scenario A) is a **`17σ` detection** at CMB-S4 precision

**If CMB-S4 measures `N_eff = 3.046 ± 0.03`:** The mirror sector is
absent at the needed level. The framework's cosmological sector
reduces to the minimal prediction (`N_eff = 3.09`, `H₀ ≈ 67.7`,
`S8 ≈ 0.80`). This would be a partial falsification.

**If CMB-S4 measures `N_eff = 3.3–3.5`:** The mirror sector is
confirmed. Every other prediction in this table follows.

**Why N_eff cannot be dialled away.** The hidden sector coupling
cannot be adjusted to reduce `N_eff` toward 3.046 without destroying
the dark matter explanation. The two predictions are structurally
linked through `ξ`:

    N_eff ≈ 3.046 + 0.49 × 6.14 × ξ⁴   and   Ω_DM/Ω_b ≈ 1/ξ²

Any value of `ξ` consistent with `Ω_DM/Ω_b ≈ 5` (giving `ξ ≈ 0.43–0.47`)
predicts `N_eff ≈ 3.3–3.4`. The only way to obtain `N_eff ≈ 3.046` with
this framework is `ξ → 0` (no mirror sector), which then gives
`Ω_DM/Ω_b → ∞` — inconsistent with observation.

The framework therefore makes the following falsifiable joint
prediction: IF a hidden mirror sector explains dark matter via the
`1/ξ²` law, THEN `N_eff` must be elevated above the SM value at precisely
the level detectable by CMB-S4. If CMB-S4 finds `N_eff = 3.046 ± 0.03`,
the mirror sector explanation of dark matter is excluded at `> 9σ`.

---

## I.3 DESI DR3: The Expansion History Test

**Target:** `σ(H(z)) ~ 0.5%` at each redshift bin
**Expected:** 2027
**Framework prediction:** `H(z) × r_d` elevated by 3–3.4% at `z = 0.5–0.9`;
`w₀ = −1`, `w_a = 0` (frozen dilaton; Paper 6 §2; Appendix F).

With `w = −1`, the `H(z)` deviations from `ΛCDM` come from elevated
`N_eff` and lower `Ω_m` (not from evolving `w`). At DESI DR3 precision:
- The `H₀` shift (69.5 vs 67.4) is detectable via the BAO scale
- The 3.2–3.4% excess in `H(z) × r_d` at `z = 0.51–0.93` is a
  **`6–7σ` signal** in individual DESI DR3 bins (see Appendix B Table B4)
- The `r_d` shift (146.2 vs 147.1 Mpc) is distinguishable at **`1.8σ`**

If DESI DR3 confirms `w ≠ −1` at `> 5σ`, non-perturbative modifications
to the dilaton potential are required (a significant revision). The
framework's `w = −1` prediction, combined with elevated `N_eff` and lower
`Ω_m`, produces a distinctive `H(z)` signature that cannot be produced
by any `ΛCDM` model.

---

## I.4 Euclid + Rubin: The `S8` Test

**Target:** `σ(S8) ~ 0.005`
**Expected:** 2027–2028
**Framework prediction:** `S8 = 0.753`

At Euclid/Rubin precision:
- Framework `S8 = 0.753` vs `ΛCDM` `S8 = 0.832` is **`16σ`**
- Framework `S8 = 0.753` vs current WL measurements (`0.76 ± 0.02`)
  is `< 1σ` — consistent
- The shape of the matter power spectrum `P(k)` differs from `ΛCDM`
  at small scales (from collisional mirror DM)

If Euclid measures `S8 = 0.75–0.77`: decisive confirmation of the
framework's `S8` prediction.

If Euclid measures `S8 = 0.80–0.83`: the framework's `S8` mechanism
is falsified.

---

## I.5 JUNO: The Neutrino Ordering Test

**Target:** Mass ordering at `> 3σ`
**Expected:** 2028–2031
**Framework prediction:** Normal ordering (`m₃ > m₂ > m₁`), with
`Σm_ν = 0.06` eV and `m₁ → 0` (normal hierarchy minimum)

**Source of the prediction.** From Paper 1, Appendix Z: the `Z₃`
geometry of the orbifold imposes a cyclic permutation symmetry on the
three generations of bulk right-handed neutrinos. The `Z₃` action on
their wavefunctions (phases `ω, ω², 1` where `ω = e^{2πi/3}`) generates
a neutrino mass matrix whose eigenvalue ordering in the 4D effective
theory is `m₁ < m₂ < m₃` (normal ordering). Inverted ordering requires
either breaking `Z₃` or introducing a hierarchy in the bulk masses —
neither of which is present in the minimal orbifold. The normal
ordering prediction is robust as long as the `Z₃` symmetry is exact
(forced by the orbifold geometry, not an assumption).

JUNO is currently taking data (since August 2025) and published first
oscillation results in November 2025. The ordering determination is
expected within the 6-year run.

**The three-experiment neutrino test.** JUNO alone tests the ordering
prediction. The combination of three experiments provides a highly
specific corroboration:

1. **JUNO (ordering):** Normal ordering confirmed if `m₃ > m₂ > m₁`.
2. **KATRIN (lightest mass):** `m₁ → 0` (below KATRIN's `0.2 eV` threshold,
   consistent with framework; KATRIN non-detection is expected).
3. **CMB-S4 (mass sum):** `Σm_ν ≈ 0.06 eV` at sub-meV precision. The
   framework predicts the normal hierarchy minimum, not a higher
   `Σm_ν ≈ 0.10–0.12 eV`.

The joint signature — normal ordering + `m₁ → 0` + `Σm_ν ≈ 0.06 eV` —
is unique to this framework among viable BSM scenarios.

**If JUNO finds normal ordering:** Consistent. Combined with
KATRIN and CMB-S4, this provides strong corroboration.

**If JUNO finds inverted ordering:** The simple `Z₃` bulk seesaw
is falsified.

---

## I.6 MEMS/Torsion Balance: The Short-Range Gravity Test

**Target:** Deviation from Newton's law at `λ ~ 10–50 μm`
**Expected:** 3–7 years
**Framework prediction:** Yukawa deviation at `λ ~ 21 μm` (circle)
or `λ ~ 12 μm` (orbifold)

This tests the e-circle radius directly. A positive detection
at the predicted length scale would be the most dramatic
confirmation of the entire framework.

Current bound (Lee et al. 2020): no deviation above `52 μm`.
The predicted scale (`12–21 μm`) is untested. Next-generation
MEMS experiments target this range.

---

## I.7 Gravitational Wave Standard Sirens

**Target:** `σ(H₀) ~ 2%` by O5 (GW + EM counterpart)
**Expected:** 2027–2028
**Framework prediction:** `H₀ = 69.5 km/s/Mpc`

GW standard sirens are independent of both the distance ladder
and CMB modeling. At 2% precision, they can distinguish `H₀ = 69.5`
from `H₀ = 67.4` (`ΛCDM`) at `3σ` and from `H₀ = 73.0` (SH0ES) at `5σ`.

The framework's prediction (69.5) is squeezed between the two
current measurements. A GW measurement of `H₀` in the range 68–71
would strongly support the framework.

---

## I.8 ADMX / ABRACADABRA / IAXO: The Axion Test

**Target:** QCD axion in the `μeV – meV` range
**Expected:** Ongoing
**Framework prediction:** No QCD axion (`π₄(SU(3)) = 0`)

From Paper 1, Appendix X: the 5D theta parameter is absent
(topologically enforced by `π₄(SU(3)) = 0`). No Peccei-Quinn
axion is needed or expected.

**If any axion experiment finds the QCD axion:** The topological
strong CP resolution is definitively falsified.

---

## I.9 The Correlated Test Strategy

The framework's predictions are CORRELATED: they all follow from
the same four geometric inputs `{L, ξ, V(φ), SM content}`. This
means:

**If CMB-S4 finds `N_eff = 3.39`** → `ξ ≈ 0.47` is confirmed → `H₀`
prediction is confirmed → `S8` prediction is confirmed → the entire
cosmological sector is validated simultaneously.

**If CMB-S4 finds `N_eff = 3.046`** → `ξ ~ 0` (no mirror sector) →
`H₀` prediction fails → `S8` mechanism is undermined → multiple
predictions fail simultaneously.

The framework is either right in a correlated way or wrong in a
correlated way. This is the hallmark of a genuine theory, not a
collection of post-hoc explanations.

---

## I.10 Summary Timeline

| Year | Experiment | Framework test | Decision |
|------|-----------|----------------|---------|
| 2025–2028 | JUNO | Neutrino mass ordering | Normal vs inverted |
| 2027 | DESI DR3 | `H(z)` shape, `w(z)` | `8σ` test of `H(z)` peak |
| 2027–2028 | LDMX/LHCb | Dark photon `ε ~ 5×10⁻⁴` | Detection or exclusion |
| 2027–2028 | Euclid | `S8` | `16σ` test (vs `ΛCDM`) |
| 2027–2028 | GW O5 | `H₀` to 2% | `3σ` vs `ΛCDM`, `5σ` vs SH0ES |
| 2028–2030 | MEMS gravity | Yukawa at `λ~12 μm` | Detection or exclusion |
| 2030 | CMB-S4 | `N_eff = 3.39` | `11σ` confirmation or `3σ` exclusion |
| 2030 | CMB-S4 | `θ*` shift | `17σ` test |
| 2030+ | Stellar ages (JWST) | `t₀ = 13.47 Gyr` | 100 Myr precision |

**The decade 2025–2035 will definitively test every prediction
in this paper.** The framework makes no room to hide.
