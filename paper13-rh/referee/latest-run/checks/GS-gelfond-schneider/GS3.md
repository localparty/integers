# GS3: Argument works for exact formula, not just first-order
**PASS.** The exact formula gives p^{-2*delta} = (np-k)/(n-k), an explicit algebraic relation. The argument uses this exact expression, not the Taylor approximation.
