# BC4: T_BC correctly defined on H_R
**FAIL.** T_BC is defined by fiat (Axiom 1: T_BC e_n = gamma_n e_n) rather than constructed from the BC algebra. The connection between the BC scaling operator and the operator with Riemann zero eigenvalues is assumed, not proved.
