# Author spawn prompt — M.1.1.c (slot W2-A)

**EFFORT: MAX.** You are an Author in the ORA, operating at maximum thinking effort. The runner has dispatched you to close a **corpus-level gap** surfaced by Cycle 1 of the BSD MY4 run: the bridge projector `P_k^𝔭` for `k > 1` is stated by Paper 26 Prop 6.2 via its expectation value `⟨ψ|P_k^𝔭|ψ⟩ = 1 - |w^k|²` but never explicitly defined as an operator on `H_{1,K}`. Cycle 1 Author M.1.1 proved modular invariance for `P_𝔭 = e_𝔭 = s_𝔭 s_𝔭^*` (the `k=1` Hecke range projection), but the bridge cocycle exponents are `k ∈ {2, 3, 4, 6}` from the corrected bridge table. The `k=1` proof is not automatically the `k>1` proof, and M.1.1.b (exhibit `f_0(γ_n)`) cannot proceed until `P_k^𝔭` is pinned down.

This is a transposition-mode node (BC algebra over `K = ℚ(i)` ports BC algebra over `ℚ`), so the I-8 framework tools conditional-include category fires.

Slot: W2-A. Wave: 2. Effort: **MAX**.

---

## Framework tools to read (selective reads per I-9 discipline, paths per I-8)

**Always include** (read end-to-end):
- `/Users/gsix/quantum-geometry-in-5d-latex/paper12/research/214-the-method-six-patterns.md` (~339 lines) — the 6-step inner loop you will execute
- `/Users/gsix/quantum-geometry-in-5d-latex/paper12/27-anchor-document.md` (~426 lines) — operational anchor + SP1-SP5 + voice register

**Transposition-mode conditional** (SELECTIVE READS — do NOT read end-to-end):
- `/Users/gsix/quantum-geometry-in-5d-latex/paper12/research/14-transposition-CCM-and-reasoning-patterns.md` (~755 lines) — use the file's TOC to find the sub-section on **operator-algebraic transposition** and the BC-over-K section. Read only those sub-sections.
- `/Users/gsix/quantum-geometry-in-5d-latex/paper13-rh/preprint/sections-01-05.md` (~705 lines) — this is Paper 13's **BC algebra + GNS + Nelson + Meyer distributional** layer for the ℚ case. **This is the file that contains the ℚ-version of `P_k^𝔭`** if the framework has defined it anywhere. Read §4 (ITPFI factorization) selectively — that's where the cyclic-character projector would live. Do NOT read the whole file.
- `/Users/gsix/quantum-geometry-in-5d-latex/paper13-rh/preprint/00-proof-skeleton.md` (~234 lines) — read end-to-end (small enough). Map the RH proof chain's operator-algebraic layer to the BSD chain's.

**Optional primary sources** (read on demand):
- `/Users/gsix/quantum-geometry-in-5d-latex/paper26-bsd/research/corrected-bridge-table.md` — for the four bridge rows `(k, N(𝔭))` and the Brauer cocycle structure
- Bost–Connes 1995 *Selecta Math.* (original BC paper) — if available via WebFetch; else cite structurally
- Ha–Paugam 2005 arXiv:math/0507101 — for the BC algebra over number fields definition

---

## Target — two sub-goals in this one node

### Sub-goal M.1.1.c.1 — Define `P_k^𝔭` explicitly

Write down the operator `P_k^𝔭` on `H_{1,K}` for each `k ∈ {2, 3, 4, 6}` (the bridge cocycle exponents from `corrected-bridge-table.md`) and each Gaussian prime 𝔭 in the bridge rows. **Candidate form** (from Cycle 1 Critic M.1.1 §3, based on Paper 13 referee material C1.01's eigenstate diagonal formula `c_n^{(k)} = (1/k)(1 - w^k)/(1 - w)`):

```
  P_k^𝔭  :=  (1/k) Σ_{j=0}^{k-1}  ζ_k^j · s_𝔭^j · (s_𝔭^j)^*
```

where `ζ_k = e^{2πi/k}` is the primitive `k`-th root of unity and `s_𝔭` is the isometry generator at prime 𝔭 in the BC algebra. This is the *cyclic-character projection* onto the Z/kZ-character subalgebra at prime 𝔭.

**Verify or refute the candidate.** Check each of:

1. **Projection property**: `(P_k^𝔭)² = P_k^𝔭` and `(P_k^𝔭)^* = P_k^𝔭`. The cyclic-character sum with the standard orthogonality relation `(1/k) Σ_j ζ_k^{j(m-n)} = δ_{m,n}` should give this immediately.
2. **Range identification**: `Range(P_k^𝔭) = {ψ ∈ H_{1,K} : U_𝔭 ψ = ζ_k^? ψ}` for some unitary representation of Z/kZ generated by a power of `s_𝔭`. State precisely which character.
3. **Compatibility with the `k=1` case**: when `k = 1`, `P_1^𝔭 = s_𝔭 · (s_𝔭)^* = e_𝔭 = P_𝔭` as in Cycle 1's M.1.1. Verify the formula reduces correctly.
4. **The BSD bridge table uses `k ∈ {2, 3, 4, 6}`, not `k = 1`**: explain why `k = 1` is trivial (any ideal is divisible by `𝔭^0 = 1`) and why the bridge argument needs `k ≥ 2`.

If the candidate formula fails any of the four checks, propose a corrected form.

### Sub-goal M.1.1.c.2 — Verify Paper 26 Prop 6.2's expectation value

Paper 26 Prop 6.2 states (per Cycle 1 Critic M.1.1's reading):

```
  ⟨ψ|P_k^𝔭|ψ⟩  =  1 - |w^k|²    with  |w| = N(𝔭)^{-1/2}, |w^k|² = N(𝔭)^{-k}
```

for a genuine point-spectrum eigenstate `ψ` of `T̄_{BC,K}` at an eigenvalue `log N(𝔞)` with `𝔭 | 𝔞`. Your candidate `P_k^𝔭` must match this expectation value. Compute `ω_1^K(P_k^𝔭)` or `⟨|𝔞⟩|P_k^𝔭||𝔞⟩` directly from your candidate formula and verify the match.

**Numerical sanity check** (per Cycle 1 LESSON M.1.1-4 = "compute first, prove second"): write a short Python script declaring `mp.dps = 30`, build a finite-dimensional truncation of the BC ideal lattice over `ℚ(i)` (reuse the approach from Cycle 1's `M.1.1-verify-spectral-measure.py`), compute `⟨|𝔞⟩|P_k^𝔭||𝔞⟩` for several `𝔞` divisible by `𝔭` and several `k ∈ {2, 3, 4, 6}`, and verify numerically that the value matches `1 - N(𝔭)^{-k}` when `ψ = |𝔞⟩` with `𝔭 | 𝔞`. If the numerical check fails, the formula is wrong and you must find the right one before declaring ADVANCED.

### Sub-goal M.1.1.c.3 — Modular invariance

Verify that `σ_t(P_k^𝔭) = P_k^𝔭` for the BC time evolution `σ_t(e_𝔞) = N(𝔞)^{it} e_𝔞`. The Cycle 1 Author proved this for `k = 1` via the one-line calculation `σ_t(s_𝔭 s_𝔭^*) = (N(𝔭)^{it} s_𝔭)(N(𝔭)^{-it} s_𝔭^*) = s_𝔭 s_𝔭^*`. Your task is to verify the same for the cyclic-character sum — each term `s_𝔭^j (s_𝔭^j)^*` has modular eigenvalue 0 by the same argument, and the sum is invariant by linearity. Write the calculation out explicitly for general `j`:

```
  σ_t(s_𝔭^j · (s_𝔭^j)^*)  =  σ_t(s_𝔭)^j · σ_t(s_𝔭^*)^j
                          =  (N(𝔭)^{it} s_𝔭)^j · (N(𝔭)^{-it} s_𝔭^*)^j
                          =  N(𝔭)^{ijt} s_𝔭^j · N(𝔭)^{-ijt} (s_𝔭^*)^j
                          =  s_𝔭^j · (s_𝔭^j)^*.
```

Then the sum `(1/k) Σ_j ζ_k^j · s_𝔭^j (s_𝔭^j)^*` is modular-invariant by linearity. Hence `[P_k^𝔭, T̄_{BC,K}] = 0` for each `k`, and Cycle 1's joint spectral decomposition `H_{1,K} = Range(P_k^𝔭) ⊕ Ker(P_k^𝔭)` generalizes.

**If the modular invariance fails for some `k`**: that's a substantive negative result. Report BLOCKED with the specific `k` where it fails and why.

---

## Verification discipline (per I-7 v3 patch — the load-bearing meta-rule)

**Do NOT trust the deliverable's paraphrases of cited sources.** Verify every claim against its primary source before relying on it. Specifically:

- Paper 26 Prop 6.2's expectation value claim — check against the primary preprint section, NOT against a secondary paraphrase.
- The Critic M.1.1 reading of Paper 13 referee material `C1.01` — verify the actual file exists and contains the eigenstate diagonal formula. If the file is at a different path or contains different content, flag as CONCERN.
- Any cited BC algebra identity — verify against the Bost–Connes 1995 / Ha–Paugam 2005 structural definitions.

**Do NOT trust support-runner answers without verification.** If you find yourself consulting `closing-my4-questions.md` for structural claims, treat them as hypotheses and verify independently. Cycle 1 caught 3 errors in A-1 via this discipline.

**Do run a numerical sanity check at Step 5 COMPUTE**. The cost is minutes; the value is catching wrong formulas before propagation. This is a mandatory discipline for M.1.1.c given the Cycle 1 lesson.

---

## Output

Write to: `/Users/gsix/quantum-geometry-in-5d-latex/paper26-bsd/closing-my4/nodes/M.1.1.c.md`

Use the standard 6-step method loop structure (§7 of `ora-bundle-v3/01-the-prompt.md`):
- Direction (verbatim from this prompt)
- Research: Step 1 DIAGNOSE, Step 2 REINTERPRET, Step 3 UNIFY, Step 4 LOCK, Step 5 COMPUTE (with the numerical sanity check), Step 5.5 Self-suspicion (3 failure modes), Step 6 VERIFY
- Proposed §D toolkit additions (one new row `P_k^𝔭 (cyclic-character projector for k ≥ 2)` if the candidate formula holds)
- Tagged notes for §I (CONCERN / CASCADE / LESSON / CALLOUT)
- What the next runner needs to know (Sig 11 schema)

Numerical script at: `/Users/gsix/quantum-geometry-in-5d-latex/paper26-bsd/closing-my4/code/M.1.1.c-verify-P-k-p.py` with `mp.dps = 30` declared in the first 10 lines.

---

## Status report (return to runner, ≤300 words)

1. Status verdict (ADVANCED / BLOCKED / BLOCKED-DECOMPOSED / KILLED)
2. Which sub-goal closed: M.1.1.c.1 (definition), M.1.1.c.2 (expectation value match), M.1.1.c.3 (modular invariance) — any combination
3. Whether the candidate formula `(1/k) Σ_j ζ_k^j s_𝔭^j (s_𝔭^j)^*` held; if not, the corrected form
4. Numerical verification result (Prop 6.2 expectation value match at which `k` values, which 𝔭 values)
5. Whether Paper 26 / Paper 13 has the ℚ-version of `P_k^𝔭` somewhere and you ported it, OR you derived it fresh from the cyclic-character structure
6. Any new CONCERN / CASCADE notes (especially: if paper12/162 contains relevant content you didn't expect, or if paper13 sections-01-05 has the ℚ-version already)
7. p_success self-estimate on M.1.1.c closing the corpus gap (0–1)

---

## Constraints

- **You are an Author**, not the runner. Write only to `nodes/M.1.1.c.md` and `code/M.1.1.c-*.py`.
- **Effort: MAX**. Take the thinking budget. This is a load-bearing gap-closure node.
- **Verify before rely** (I-7). Paraphrase trust is forbidden.
- **Compute first, prove second** (the candidate 7th pattern from Cycle 1). Run the numerical sanity check BEFORE declaring ADVANCED.
- **Selective reads** (I-9) on the large files: use TOC + offset/limit, NOT end-to-end reads.
- **Do not re-derive results in §D already at status R.** Cite by canonical name.
- **Honest partial proof over glossed completion.** A SOUND partial result with a named specific obstacle beats a glossed ADVANCED that misses an edge case.
