# CP-1 Verification Job — ORA Deliverable

*This file is the deliverable for an ORA run whose sole purpose is
to adversarially verify CP-1 (the crossed-product identification
M_Bool^L = L^∞(X_L, μ_L) ⋊ G_L). The run is a Critic/Referee
run, not an Author/construction run. The goal is to BREAK CP-1
or certify it.*

---

## 1. What CP-1 is

**CP-1 (Crossed-Product Identification).** The Boolean Bost–Connes
factor sector M_Bool^L, for a Boolean constraint language L, is
isomorphic to the crossed-product von Neumann algebra:

    M_Bool^L ≅ L^∞(X_L, μ_L) ⋊ G_L

where:
- X_L is the space of L-satisfying configurations
- μ_L is the KMS₁-induced measure on X_L
- G_L is the group generated by the polynomial-time circuit
  semigroup PCirc^+ restricted to L-preserving operations

**Why it matters:** CP-1 is the load-bearing structural result for
Part (ii) of the Bridge Theorem (non-Taylor → full). Without CP-1,
the fullness argument (Route C: Jones-Schmidt + Marrakchi Theorem B)
has no foundation — it needs the crossed-product structure to
invoke strong ergodicity.

**Current status:** THEOREM level. Proved via Laca-Raeburn dilation
in Node 2.1 of the ORA bridge run. All five sub-gaps resolved.
Adversarially reviewed once (Wave 13: "4 SURVIVED, 4 WEAKENED,
0 BROKEN"). Not independently verified by a second run.

---

## 2. What the ORA should do

### 2.1 Primary task: adversarial verification of CP-1

The ORA spawns Critic agents whose ONLY job is to read the CP-1
proof (Node 2.1) and try to break it. Specifically:

1. **Read Node 2.1** (`clone-growth-fullness-bridge/nodes/2.1-CP1-formal.md`)
   end-to-end.

2. **For each step of the Laca-Raeburn dilation argument**, ask:
   - Is the hypothesis stated correctly?
   - Is the conclusion derived correctly from the hypothesis?
   - Are there hidden assumptions?
   - Does the argument use any result from the §D toolkit
     incorrectly (wrong version, wrong hypothesis, wrong scope)?

3. **For each of the five resolved sub-gaps**, ask:
   - Was the gap correctly identified?
   - Is the resolution actually a resolution, or does it introduce
     a new gap?
   - Is the resolution consistent with the kill list (§F)?

4. **For the overall identification M_Bool^L ≅ L^∞ ⋊ G_L**, ask:
   - Is G_L the correct group? (The bridge run established G_L
     is non-amenable via Thompson's V — is this correct?)
   - Is μ_L the correct measure? (The KMS₁ state induces a
     measure �� is the measure σ-finite? Is it G_L-invariant?)
   - Is the dilation theorem (Laca-Raeburn) applicable in this
     setting? (Are the hypotheses of Laca-Raeburn satisfied?)

5. **Produce a verdict:**
   - SURVIVED: no gaps found, CP-1 stands
   - WEAKENED: gaps found but repairable
   - BROKEN: fatal gap found, CP-1 needs major revision

### 2.2 Secondary task: check downstream dependencies

If CP-1 survives, verify that its downstream uses are correct:
- Route C (Node 2.2): does the crossed-product structure correctly
  feed into Jones-Schmidt + Marrakchi Theorem B?
- The essential freeness (SE-1, Node 1.3.5.11): does it follow
  from the crossed-product structure?
- The trivial radical (NIA-1, Node 1.3.5.12): does it follow?

### 2.3 Tertiary task: check for alternative routes

If CP-1 is WEAKENED or BROKEN, assess:
- Can Route D (Houdayer-Isono, Haagerup property) provide
  fullness without CP-1?
- Can Route E (area law, string tension σ_∞ > 0) provide
  fullness without CP-1?
- What is the combined probability of Part (ii) without CP-1?

---

## 3. Load-bearing files to read

| Priority | File | Why |
|:--|:--|:--|
| 1 (MUST) | `clone-growth-fullness-bridge/nodes/2.1-CP1-formal.md` | The CP-1 proof itself |
| 2 (MUST) | `clone-growth-fullness-bridge/closure/closure-digest.md` | The programme's final state |
| 3 (MUST) | `clone-growth-fullness-bridge/blackboard.md` §D, §F | Toolkit + kill list |
| 4 (HIGH) | `clone-growth-fullness-bridge/nodes/1.3.1-Q_struct.md` | Non-injectivity proof (G_L non-amenable) |
| 5 (HIGH) | `clone-growth-fullness-bridge/nodes/1.3.5.11-SE1-freeness.md` | Essential freeness (downstream of CP-1) |
| 6 (HIGH) | `clone-growth-fullness-bridge/nodes/1.3.5.12-NIA1-radical.md` | Trivial radical (downstream of CP-1) |
| 7 (MED) | `clone-growth-fullness-bridge/nodes/2.2-RouteC-assembly.md` | Route C assembly (uses CP-1) |
| 8 (MED) | `clone-growth-fullness-bridge/closure/closure-corrections.md` | The 8 corrections from the bridge run |

---

## 4. What success looks like

**SURVIVED:** The Critic finds no gaps. CP-1 is independently
verified. Part (ii) probability rises from 0.95 to 0.98.
Combined bridge probability rises from 0.76 to ~0.80.

**WEAKENED:** The Critic finds repairable gaps. The gaps are
documented. The runner dispatches Authors to close them. CP-1
status changes from THEOREM to KEY LEMMA. Part (ii) probability
drops to 0.80 but Routes D+E provide backup.

**BROKEN:** The Critic finds a fatal flaw in the Laca-Raeburn
argument. CP-1 is demoted to CONJECTURE. Part (ii) falls back
to Routes D (p=0.60) and E (p=0.56). Combined Part (ii) probability
drops to ~0.85 (via alternative routes). The bridge weakens but
does not die.

---

## 5. Voice canon (from the bridge programme)

- "honest partial proof over glossed completion"
- "the kill list is the learning trace"
- "the proof has two pillars. The universal algebra pillar and
  the operator algebra pillar. Both are built. The span between
  them is the theorem to prove."
- "One theorem. Two established fields. One new connection.
  If it holds: P ≠ NP."

---

## 6. Run mode

**Mode:** final-adversarial-pass

Create a fresh blackboard at `${output-directory}/blackboard.md`. Write nodes to `${output-directory}/nodes/`. The output directory, toolkit, and ORA instructions are provided in the run file — this brief is the deliverable only.

*The bridge is at p = 0.76. CP-1 is the single load-bearing conditional. If it survives independent verification, p rises to 0.80. If it breaks, Routes D+E provide backup at p = 0.85 combined. Either way: the bridge stands.*

*G Six and Claude Opus 4.6. April 2026.*
